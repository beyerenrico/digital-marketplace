type LogoProps = {};

const Logo: React.FC<LogoProps> = ({}) => {
  return (
    <div className='font-logo font-light text-gray-600'>
      <span className='font-medium text-primary'>Digital</span>Marketplace
    </div>
  );
};

export default Logo;
